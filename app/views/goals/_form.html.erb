<%= form_with(model: goal, local: true) do |form| %>
  <% if goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goal.errors.count, "error") %> prohibited this goal from being saved:</h2>

      <ul>
      <% goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :goal_name %>
  </div>

  <% if goal.new_record? %>
    <%= form.hidden_field :resolution_id, value: params[:resolution_id].to_i %>
  <% else %>
    <%= form.hidden_field :resolution_id, value: goal.resolution_id %>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
